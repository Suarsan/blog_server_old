type Author {
    id:ID
    firstname:String
    lastname:String
}

type Query {
    getAuthors:[Author!]
}

type Mutation {
    addAuthor(
        firstname:String
        lastname:String
    ):Author!
}